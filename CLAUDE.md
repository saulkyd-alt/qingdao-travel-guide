# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project State

This is a new, empty project directory. No code, dependencies, or configuration have been set up yet.

## Conventions

- **Language**: This project uses TypeScript (Node.js) with JavaScript/TSX/JSON file extensions.
- **Package manager**: npm (with `package-lock.json`)
- **Module system**: ESM (`"type": "module"` in package.json)
- Do not create documentation files (*.md) or README files unless explicitly requested.
